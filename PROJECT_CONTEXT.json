{
  "project": {
    "name": "TVAC Thermal Analyzer",
    "kicad_min_version": "9.0",
    "language": "Python (wxPython) + optional C native backend",
    "goals": [
      "TVAC thermal simulation with radiation",
      "Component power heat sources",
      "Terminal-based current injection with routing-aware Joule heating",
      "Mounting to temperature-controlled box at defined points (B.Cu)",
      "Interactive visualization overlays"
    ],
    "defaults": {
      "via_plating_thickness_um": 25.0,
      "mounting_side": "B.Cu",
      "fr4_nominal": true,
      "copper_resistivity_ohm_m_at_20c": 1.724e-8,
      "copper_tempco_per_c": 0.00393
    }
  },
  "verified_from_wip_snapshot": {
    "snapshot": "tvac_thermal_analyzer_WIP_snapshot4.zip",
    "changed_files_vs_original": [
      "__init__.py",
      "core/config.py",
      "core/pcb_extractor.py",
      "solvers/current_solver.py",
      "solvers/mesh_gen.py",
      "solvers/thermal_solver.py",
      "ui/main_dialog.py"
    ],
    "known_compile_blockers": [
      {
        "file": "core/pcb_extractor.py",
        "issue": "TraceSegment.__post_init__ has an incomplete assignment (`self.net =`) causing a syntax error in snapshot4"
      },
      {
        "file": "solvers/current_solver.py",
        "issue": "_add_pour_sheet_grids is nested (indentation/structure bug) so plane grid logic is not reliably executed"
      }
    ]
  },
  "done": [
    "Hard dependency check for NumPy/SciPy with wx error dialog",
    "Config schema extended: terminal-based CurrentPath fields, via plating thickness, plane grid params",
    "Run gating improved for current injection mode (doesn't require component power)",
    "Thermal solver structure includes robin BC fields and a radiation-linearization approach (partially implemented)"
  ],
  "partially_done": [
    {
      "area": "PCB extraction (pads/traces/vias/zones)",
      "notes": "Fields are being added (net_code, ids, lengths) but snapshot4 has a syntax error; extraction completeness still needs verification"
    },
    {
      "area": "Plane/zone electrical modeling",
      "notes": "Parameters exist; implementation direction is sheet resistance grid; structural bug prevents reliable execution"
    },
    {
      "area": "Electro-thermal coupling",
      "notes": "Config fields exist; current implementation is still closer to global/avg coupling, not local rho(T) with relaxation"
    }
  ],
  "missing_priority_A": [
    "Fix current_solver plane-grid method structure; ensure it runs",
    "Merge multiple pours per (net_code, layer)",
    "Multi-polygon and hole/void support using filled geometry where possible; documented fallback",
    "Deterministic stitching of routed nodes into plane grid; avoid double edges",
    "Validation mode metrics for current injection (Req, dV, Ptot, top dissipators) exposed in UI/log"
  ],
  "missing_priority_B": [
    "Local rho(T) using temperature lookup at segment/edge midpoints",
    "Under-relaxation and stable convergence criterion",
    "Caching to avoid rebuilding plane grids every iteration",
    "Performance checks: memory limits for small plane_grid_mm"
  ],
  "other_missing": [
    "Ensure Joule map deposition onto thermal mesh is layer-correct and energy-conserving",
    "README: full math, approximations, accuracy, and solver stability discussion"
  ]
}
